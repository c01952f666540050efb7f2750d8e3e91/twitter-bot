import dash
from dash import dcc, html, Input, Output, State, dcc, ctx, callback, register_page
import dash_bootstrap_components as dbc

# Register Page
register_page(__name__)

# Layout
layout = html.Div(
    children=[
        html.P("aksdfasdfasd"),
        html.Br(),
        html.P("asdfasdfads")
    ]
)